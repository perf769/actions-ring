<Window x:Class="ActionsRing.App.Windows.ProfileNameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Название профиля"
        Width="440" Height="230" MinWidth="380" MinHeight="220"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False"
        Background="{DynamicResource WindowBrush}">
    <Grid Margin="24,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="HeadingText" Text="Новый профиль" Style="{StaticResource SectionTitleText}" />
        <TextBlock x:Name="DescriptionText" Grid.Row="1" Text="Введите понятное название набора колец." Style="{StaticResource CaptionText}" Margin="0,6,0,0" />
        <TextBox x:Name="NameBox" Grid.Row="2" Height="44" MaxLength="80" Margin="0,10,0,0" Padding="12,6"
                 VerticalAlignment="Top" VerticalContentAlignment="Center" />
        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <TextBlock x:Name="ValidationText" Foreground="{DynamicResource DangerBrush}" VerticalAlignment="Center" />
            <Button x:Name="CancelButton" Grid.Column="1" Style="{StaticResource SecondaryButton}" Content="Отмена" IsCancel="True" MinWidth="92" />
            <Button Grid.Column="2" Style="{StaticResource PrimaryButton}" Content="Сохранить" Click="OnSave" MinWidth="104" Margin="9,0,0,0" />
        </Grid>
    </Grid>
</Window>
