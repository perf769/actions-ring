<p align="center">
  <img src="./Assets/readme/hero.svg" width="100%" alt="Actions Ring — контекстное кольцо действий для Windows с профилями, приложениями и подменю">
</p>

<p align="center">
  <a href="https://github.com/perf769/actions-ring/releases/latest"><strong>Скачать для Windows</strong></a>
  &nbsp;&nbsp;·&nbsp;&nbsp;
  <a href="https://github.com/perf769/actions-ring/releases/tag/macos-v0.2.0">Скачать для Mac · Preview</a>
  &nbsp;&nbsp;·&nbsp;&nbsp;
  <a href="https://github.com/perf769/actions-ring">Исходный код</a>
</p>

<p align="center">
  <code>Windows x64</code>&nbsp;&nbsp;·&nbsp;&nbsp;<code>WPF</code>&nbsp;&nbsp;·&nbsp;&nbsp;<code>.NET 10</code>&nbsp;&nbsp;·&nbsp;&nbsp;работает локально
</p>

## Actions Ring в работе

Подменю, профили приложений и оформление — за 46 секунд. Включите звук в плеере.

https://github.com/user-attachments/assets/65cd520e-32ca-4445-ab56-6b113272749c

[Открыть видео отдельно](https://github.com/user-attachments/assets/65cd520e-32ca-4445-ab56-6b113272749c) · [Музыка и лицензия](./Assets/readme/presentation-license.txt)

## Команды всегда рядом с курсором

Actions Ring показывает радиальное меню поверх активного приложения. Вызовите его кнопкой мыши или клавишей, наведитесь на нужный пузырь и отпустите кнопку — команда выполнится в том окне, где вы работали.

- Несколько пользовательских профилей: у каждого свои приложения, кольца и оформление.
- Отдельные контексты для программ с автоматическим выбором по активному окну.
- Поиск установленных классических и Microsoft Store-приложений, включая записи из `AppsFolder`.
- Контекстные наборы команд для Adobe Photoshop, браузеров и Проводника.
- Четыре–восемь действий в основном кольце и папки-подменю до девяти действий.
- Более 5 500 встроенных иконок с поиском, иконки установленных приложений и импорт своего SVG, PNG, ICO или JPG.
- Общая палитра кольца и индивидуальные цвета каждого пузыря, наведения и иконки.
- Адаптивный размер с учётом рабочего пространства и DPI монитора под указателем.
- Встроенные обновления с проверкой контрольной суммы SHA-256 и подтверждением перед каждой установкой.

Возможности ниже относятся к версии Windows. Для **Apple Silicon, macOS Sonoma 14 и новее** доступна отдельная нативная версия: [установка и возможности macOS](./macOS/README.md). Она использует системные команды Mac и поддерживает вызов с клавиатуры, мыши или сочетанием модификаторов с прокруткой трекпада.

## Как выбирается кольцо

<p align="center">
  <img src="./Assets/readme/workflow.svg" width="100%" alt="Actions Ring принимает ввод, выбирает пользовательский профиль и контекст активного приложения, затем открывает нужное кольцо">
</p>

Сначала используется выбранный пользовательский профиль. Внутри него Actions Ring сопоставляет активное окно с контекстами приложений по имени процесса, пути к программе или заголовку окна. Если совпадений нет, открывается глобальное кольцо этого же профиля.

## Быстрый старт

Готовая сборка самодостаточна: устанавливать .NET на целевой компьютер не нужно.

1. Откройте [последний релиз](https://github.com/perf769/actions-ring/releases/latest) и скачайте `ActionsRing-portable.zip`.
2. Распакуйте архив и запустите `ActionsRing.exe` либо установите программу для текущего пользователя:

   ```powershell
   powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Install.ps1
   ```

3. Пройдите короткую первоначальную настройку и назначьте удобный способ вызова.

Программа устанавливается без прав администратора в `%LOCALAPPDATA%\Programs\ActionsRing`. Удалить её можно через Windows «Установленные приложения» или скриптом `Uninstall.ps1`.

## Профили и приложения

Пользовательский профиль — самостоятельное рабочее пространство. Например, в профиле «Работа» можно собрать кольца для Photoshop, браузера и Проводника, а в профиле «Личное» оставить другой набор приложений и команд.

При добавлении контекста можно:

- выбрать найденное приложение из каталога Windows;
- использовать приложение Microsoft Store без поиска скрытого исполняемого файла;
- добавить текущее активное окно;
- указать программу вручную.

Для каждого добавленного приложения создаётся точное правило сопоставления; если совпало несколько контекстов, используется контекст с более высоким приоритетом.

Верхняя лента приложений прокручивается колесом мыши. Обычные программы и приложения MSIX определяются по типу установки, без повторного добавления классических программ как Microsoft Store-приложений.

В карточках приложений и действиях запуска используются иконки самих программ. Для известных сервисов доступны узнаваемые векторные знаки, которые подстраиваются под цвет пузыря. Для остальных веб-ссылок Actions Ring загружает и сохраняет фавикон сайта.

## Действия и подменю

Библиотека разделяет общие и контекстные команды. При настройке кольца Photoshop в ней появляются инструменты вроде кисти, пипетки, выделения и масштаба; для браузеров — вкладки, адресная строка и обновление; для Проводника — папки, поиск, свойства и представление.

Кроме готовых команд доступны:

- сочетания и последовательности клавиш;
- ввод текста;
- запуск приложений, файлов, папок и веб-ссылок;
- системные, оконные, медиа- и мышиные действия;
- регулировка громкости, яркости, масштаба и прокрутки колесом;
- папка, которая раскрывает отдельную дугу дочерних пузырей;
- мульти-действие из нескольких шагов с задержками.

Кнопка «По умолчанию» восстанавливает исходный набор пузырей выбранного кольца.

У пузыря с подменю можно назначить отдельное действие по клику: например, нажатие открывает приложение, а наведение раскрывает его быстрые команды. Дочерние пузыри появляются плавно, отделяясь от основного пузыря.

В редакторе выбранный пузырь остаётся выделенным. При открытом подменю остальные пузыри приглушаются, но остаются доступными для выбора. Повторное нажатие снимает выделение и закрывает подменю.

Перетащите пузырь на другой в том же кольце или подменю, чтобы поменять их местами. Вместе с ним перемещаются действие, иконка, цвета и вложенное подменю. Escape или отпускание вне другого пузыря отменяют перенос.

Подписи в настройках располагаются без пересечений. В тесном окне подпись под указателем получает приоритет. Действие по клику для родительского пузыря выбирается в отдельном каталоге с поиском и категориями.

## Иконки для любых действий

Выберите пузырь и нажмите «Иконка». Доступны три источника:

- **Библиотека** — более 5 500 локальных векторных иконок, категории и поиск на русском и английском.
- **Приложения** — иконки программ, установленных на компьютере, включая Microsoft Store.
- **Свой файл** — SVG, PNG, ICO или JPG. Файл копируется в локальное хранилище иконок.

Так можно различать папки со скриншотами, команды записи экрана и сочетания клавиш даже тогда, когда они выполняют похожие действия. Кнопка «Автоматически» возвращает подбор иконки по назначенному действию. Векторные иконки следуют цветам темы; малоконтрастные растровые иконки получают контрастную подложку.

## Вызов и управление

По умолчанию кольцо назначено на дальнюю боковую кнопку мыши — `XButton2`. В настройках можно записать другую клавишу клавиатуры, сочетание с `Ctrl`/`Shift`, левую, правую, среднюю или боковую кнопку мыши, а также вертикальное и горизонтальное колесо.

Доступны два режима:

- **Удерживать** — кольцо открыто, пока нажата назначенная кнопка.
- **Переключать** — первое нажатие открывает кольцо, повторное закрывает.

Одиночные `Alt` и клавиши Windows можно назначить отдельно. Сочетания с ними не назначаются на вызов кольца, чтобы не пересекаться с системными меню; внутри действий эти модификаторы доступны.

## Оформление и движение

Внешний вид можно выбрать из готовых стилей или собрать собственную палитру в RGB/HEX-редакторе. Отдельно настраиваются фон пузыря, состояние наведения, цвет иконки и цвет иконки при наведении — для всего кольца либо для конкретного действия.

Для индивидуального оформления выберите пузырь и нажмите «Цвета». При изменении готового стиля создаётся своя тема с сохранением его исходной палитры; остальные пузыри не меняют цвет.

Предпросмотр в редакторе остаётся компактным и стабильным. Реальное кольцо масштабируется отдельно для монитора под курсором, учитывая его разрешение, рабочую область и DPI.

Параметры движения работают по-разному:

- **Анимации** полностью включают или отключают переходы.
- **Уменьшение движения** сохраняет короткие плавные появления, но убирает заметное разлетание и масштабирование элементов.

Экранные индикаторы показывают состояние `Caps Lock`, `Num Lock` и `Scroll Lock` и плавно исчезают. Их можно отключить в общих настройках.

## Работа в Windows

Actions Ring сворачивается в системный трей и может запускаться вместе с Windows. Контекстное меню трея следует светлой или тёмной теме приложения и сохраняет быстрый доступ к кольцу, настройкам, паузе, автозапуску и выходу. Переключатель автозапуска регистрируется для текущего пользователя и отображается в «Автозагрузке приложений» Диспетчера задач.

Настройки сохраняются в `%LOCALAPPDATA%\ActionsRing\settings.json`. Конфигурацию можно экспортировать в JSON и импортировать обратно; при сохранении поддерживается резервная копия.

## Обновления

На вкладке «О программе» отображается установленная версия и доступна ручная проверка официальных релизов. Автоматическая проверка и фоновая загрузка управляются отдельными переключателями. Даже при включённом автообновлении приложение всегда показывает описание изменений и ждёт решения: установить, пропустить эту версию или напомнить позже.

Архив загружается только из релизов этого репозитория, проверяется по опубликованной контрольной сумме SHA-256, распаковывается в изолированную папку и устанавливается после закрытия текущего процесса. При обновлении из портативной копии новая версия устанавливается для текущего пользователя в `%LOCALAPPDATA%\Programs\ActionsRing`; исходная портативная копия не удаляется.

## Сообщить об ошибке

Откройте **О программе → Сообщить об ошибке**. В браузере появится черновик [GitHub Issue](https://github.com/perf769/actions-ring/issues) с версией Actions Ring и операционной системы. Добавьте описание, шаги для повторения и при необходимости скриншот. Для отправки нужен аккаунт GitHub; сообщение публикуется только после вашего подтверждения на сайте.

Настройки, журналы, имя пользователя и пути к файлам автоматически не прикладываются. Перед публикацией скриншота скройте личные данные.

## Сборка из исходников

Понадобится [.NET SDK 10](https://dotnet.microsoft.com/download/dotnet/10.0). Из корня репозитория выполните:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\build\Build.ps1
```

Сценарий восстанавливает зависимости, собирает приложение, запускает тесты и создаёт самодостаточный single-file пакет `dist\ActionsRing-portable.zip` для `win-x64`.

Для отдельного запуска тестов:

```powershell
dotnet test .\ActionsRing.slnx
```

## Устройство проекта

```text
src/ActionsRing.Core              модель кольца, профили и конфигурация
src/ActionsRing.Platform.Windows  глобальный ввод и интеграция с Windows
src/ActionsRing.App               WPF-интерфейс, оверлей и системный трей
tests/                             модульные и визуальные проверки поведения
macOS/                             нативное приложение для Apple Silicon
```

Actions Ring работает локально: профили, назначенные команды и настройки ввода не отправляются на внешние серверы. Сеть используется только для загрузки фавиконки добавленной веб-ссылки и для обращения к инфраструктуре GitHub при проверке или загрузке обновления согласно выбранным настройкам.
